const firstWord = (prompt("Type the first word"))
const secondWord = (prompt("Type secondfirst word"))
if (firstWord.length > secondWord.length) {console.log(firstWord, secondWord);}
else if (firstWord.length < secondWord.length) {console.log(secondWord, firstWord);}

